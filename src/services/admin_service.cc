#include "admin_service.h"

#include <expected>
#include <string>

namespace masquerade {
/*
std::expected<Account, std::string> Account::create(std::string phone_number, std::string first_name, std::string last_name, std::string password) noexcept {
  return std::expected:: Account(phone_number, first_name, last_name, password);
}
*/
}  // namespace masquerade
